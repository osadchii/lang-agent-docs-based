services:
  backend:
    image: "${BACKEND_IMAGE:-ghcr.io/osadchii/lang-agent-docs-based/backend}:${BACKEND_IMAGE_TAG:-latest}"
    env_file:
      - .env
    environment:
      VIRTUAL_HOST: "${BACKEND_DOMAIN:?BACKEND_DOMAIN is required for reverse proxy}"
      VIRTUAL_PORT: "8000"
      LETSENCRYPT_HOST: "${BACKEND_DOMAIN:?BACKEND_DOMAIN is required for reverse proxy}"
      LETSENCRYPT_EMAIL: "${TRAEFIK_ACME_EMAIL:-ops@example.com}"
    container_name: lang-agent-backend
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    networks:
      - app-network

  loki:
    image: grafana/loki:3.0.0
    command: -config.file=/etc/loki/config.yml
    container_name: lang-agent-loki
    volumes:
      - loki_data:/loki
      - ./infra/loki/config.yml:/etc/loki/config.yml:ro
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://localhost:3100/ready"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    networks:
      - app-network

  promtail:
    image: grafana/promtail:3.0.0
    command: -config.file=/etc/promtail/config.yml
    container_name: lang-agent-promtail
    environment:
      DOCKER_API_VERSION: "1.44"
    volumes:
      - ./infra/promtail/config.yml:/etc/promtail/config.yml:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - promtail_positions:/var/lib/promtail
    depends_on:
      loki:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - app-network

  grafana:
    image: grafana/grafana:12.1.3
    container_name: lang-agent-grafana
    environment:
      GF_SECURITY_ADMIN_USER: "${GRAFANA_ADMIN_USER:-admin}"
      GF_SECURITY_ADMIN_PASSWORD: "${GRAFANA_ADMIN_PASSWORD:-admin}"
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_AUTH_ANONYMOUS_ENABLED: "false"
      GF_SERVER_DOMAIN: "${GRAFANA_DOMAIN:?GRAFANA_DOMAIN is required}"
      GF_SERVER_PROTOCOL: "http"
      GF_SERVER_ROOT_URL: "https://${GRAFANA_DOMAIN:?GRAFANA_DOMAIN is required}/"
      VIRTUAL_HOST: "${GRAFANA_DOMAIN:?GRAFANA_DOMAIN is required}"
      VIRTUAL_PORT: "3000"
      LETSENCRYPT_HOST: "${GRAFANA_DOMAIN:?GRAFANA_DOMAIN is required}"
      LETSENCRYPT_EMAIL: "${TRAEFIK_ACME_EMAIL:-ops@example.com}"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./infra/grafana/provisioning/datasources:/etc/grafana/provisioning/datasources:ro
      - ./infra/grafana/provisioning/dashboards:/etc/grafana/provisioning/dashboards:ro
    depends_on:
      loki:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - app-network

  nginx:
    image: nginxproxy/nginx-proxy:1.6.0
    container_name: nginx-proxy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
      - nginx_certs:/etc/nginx/certs
      - nginx_vhost:/etc/nginx/vhost.d
      - nginx_html:/usr/share/nginx/html
    restart: unless-stopped
    networks:
      - app-network

  nginx-acme:
    image: nginxproxy/acme-companion:2.4.0
    depends_on:
      - nginx
    container_name: nginx-proxy-acme
    environment:
      DEFAULT_EMAIL: "${TRAEFIK_ACME_EMAIL:-ops@example.com}"
      NGINX_PROXY_CONTAINER: nginx-proxy
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - nginx_certs:/etc/nginx/certs
      - nginx_vhost:/etc/nginx/vhost.d
      - nginx_html:/usr/share/nginx/html
      - nginx_acme:/etc/acme.sh
    restart: unless-stopped
    networks:
      - app-network

  db:
    image: postgres:15
    env_file:
      - .env
    container_name: lang-agent-db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - app-network

  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes
    container_name: lang-agent-redis
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    restart: unless-stopped
    networks:
      - app-network

volumes:
  postgres_data:
  redis_data:
  loki_data:
  grafana_data:
  promtail_positions:
  nginx_certs:
  nginx_vhost:
  nginx_html:
  nginx_acme:

networks:
  app-network:
    driver: bridge
